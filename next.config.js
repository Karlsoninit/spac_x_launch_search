module.exports = {
  basePath: `${
    process.env.NODE_ENV !== "development" ? "/space_x_launch_search" : ""
  }`,
  assetPrefix: `${
    process.env.NODE_ENV !== "development" ? "/space_x_launch_search" : ""
  }`,
};
